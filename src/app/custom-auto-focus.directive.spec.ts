import { CustomAutoFocusDirective } from './custom-auto-focus.directive';

describe('CustomAutoFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomAutoFocusDirective();
    expect(directive).toBeTruthy();
  });
});
